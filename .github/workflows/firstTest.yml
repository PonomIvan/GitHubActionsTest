name: Test Java App
on:
  push:
    branches: [ "master" ]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Set up JDK 20
        uses: actions/checkout@v4
      - name: Build with Maven
        run: mvn -B package --file pom.xml

